@{
    ViewBag.Title = "Welcome";
}
<main aria-labelledby="title">
    <h2 id="title">@ViewBag.Title.</h2>
    <h3>@ViewData["greeting"]</h3>
    
    <p>@Html.ActionLink("Registration For SOW Run", "Registration", "Employees", new { area = "" }, new { @class = "btn btn-primary btn-xs" })</p>

</main>